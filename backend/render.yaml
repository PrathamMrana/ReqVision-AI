services:
  - type: web
    name: reqvision-backend
    env: python
    buildCommand: "pip install -r requirements.txt && python -m nltk.downloader wordnet"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: PORT
        value: 5000
